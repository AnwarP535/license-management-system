# Database Configuration
# Note: When using docker-compose, DB_HOST should be 'postgres' (service name)
# For local development without docker, use 'localhost'
DB_HOST=postgres
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=postgres
DB_NAME=license_management

# JWT Configuration
# IMPORTANT: Change this to a secure random string in production (minimum 32 characters)
# Generate a secure secret: openssl rand -base64 32
JWT_SECRET=your-secret-key-change-in-production-min-32-chars

# Application Configuration
PORT=8080
NODE_ENV=development

# Development Notes:
# - TypeORM will auto-sync database schema when NODE_ENV=development
# - Set NODE_ENV=production for production deployments
# - In docker-compose, DB_HOST should be 'postgres' (the service name)
# - For local development, copy this file to .env and change DB_HOST to 'localhost'
